{
  "name": "orderbook",
  "version": "0.0.1",
  "description": "Display combined order book from several crypto exchanges.",
  "author": "Trey Metrailer",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm build --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "ts-node": "^6.0.0",
    "tsconfig-paths": "^3.6.0",
    "typescript": "^2.8.1"
  }
}
